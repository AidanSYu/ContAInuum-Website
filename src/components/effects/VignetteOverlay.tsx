import React from 'react';

/**
 * Deprecated on the light "Lab Instrument" theme — the dark vignette no longer
 * fits. Kept as a no-op so existing imports don't break.
 */
export const VignetteOverlay: React.FC = () => null;
