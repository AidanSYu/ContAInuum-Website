import React from 'react';

/** Very subtle paper grain — multiply-blended so it reads as texture on light. */
export const GrainOverlay: React.FC = () => {
  return <div className="grain-overlay" aria-hidden="true" />;
};
